# -----------------------------------------------------------------
# Gidh Analytics Environment Configuration
#
# INSTRUCTIONS:
# 1. Copy this file to a new file named .env in the same directory.
# 2. Fill in the values for each variable.
# 3. The .env file is ignored by git, so your secrets are safe.
# -----------------------------------------------------------------

# --- Pipeline Mode ---
# Set to 'backtesting' to run historical data.
# Set to 'realtime' for live market data (not fully implemented).
PIPELINE_MODE=backtesting

# --- Kite Connect API Credentials ---
# Required for fetching instrument lists, not for backtesting from files.
# Can be placeholder values if only backtesting.
KITE_API_KEY=your_kite_api_key
KITE_API_SECRET=your_kite_api_secret
KITE_ACCESS_TOKEN=your_kite_access_token

# --- Database Connection ---
# Connection details for your TimescaleDB/PostgreSQL instance.
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gidh_analytics
DB_NAME_BACKTEST=gidh_backtest

# --- Backtesting Configuration ---
# Absolute path to the directory containing your historical data.
BACKTEST_DATA_DIRECTORY=/path/to/your/data
# The specific date to backtest in YYYY-MM-DD format.
BACKTEST_DATE=2024-01-01
# The delay (in seconds) between sending each tick. Use 0 for max speed.
BACKTEST_SLEEP_DURATION=0.001
# Set to 'true' to clear all data from the database before a backtest run.
TRUNCATE_TABLES_ON_BACKTEST=true

# --- Data Processing Configuration ---
# The duration (in minutes) of the in-memory data window for feature calculation.
DATA_WINDOW_MINUTES=120